import numpy as np
import matplotlib.pyplot as plt

h0=float(input('Z jakiej wysokości wyrzucono ciało?'))
v0=float(input('Jaka jest wartość prędkości początkowej?'))
a0=float(input('Ile wynosi wartość przyśpieszenia grawitacyjnego?'))
theta=float(input('Pod jakim kątem wyrzucono ciało?'))